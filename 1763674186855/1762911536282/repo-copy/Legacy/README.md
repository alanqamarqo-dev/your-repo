# Legacy copies

This folder contains archived legacy implementations kept during the IntegrationRegistry migration.

Files here are read-only references and compatibility backups; they should not be used by new code.
