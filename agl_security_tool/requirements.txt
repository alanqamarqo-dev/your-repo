# AGL Security Tool v2.1.0 — المتطلبات
# ================================================

# ── Core (مطلوبة) ──
requests>=2.28.0
z3-solver>=4.12.0
numpy>=1.24.0
pydantic>=2.0.0
psutil>=5.9.0

# ── API Server ──
fastapi>=0.100.0
uvicorn>=0.22.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.0
sqlalchemy>=2.0.0
slowapi>=0.1.0
python-multipart>=0.0.5

# ── ML / Fine-tuning (اختياري — للتدريب فقط) ──
# torch>=2.0.0
# transformers>=4.30.0
# datasets>=2.14.0
# peft>=0.5.0
# trl>=0.7.0

# ── External Tools (اختياري) ──
# pip install slither-analyzer    # Slither static analysis
# pip install semgrep             # Semgrep pattern matching
# mythril                         # لا يعمل مع Python 3.13

# ── Dev ──
# pytest>=7.0.0
